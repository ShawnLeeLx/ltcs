# V26 Bug修复记录 — 2025-06-19

## 目标
从V25输出V26，修复此前分析发现的所有bug。

## 修复清单

| # | 类别 | 问题描述 | 修复方式 |
|---|------|---------|---------|
| 1 | CSS | L636 `.bld-clickable:hover` 后多一个 `}` | 删除多余 `}` |
| 2 | CSS | `@keyframes toastFade` 只有0%帧，缺100%帧和闭合 `}` | 补全 `100% { opacity: 0; } }` |
| 3 | CSS | `@keyframes fxPop` 只有0%帧 | 补全 `100% { transform: scale(1); opacity: 1; } }` |
| 4 | CSS | `@keyframes fxBurst` 只有0%帧 | 补全 `100% { transform: translate(0,0) scale(1.2); opacity: 1; } }` |
| 5 | JS | `initGame` 缺少5个波次系统变量初始化 | 添加 `_waveNoDamage:true, _waveBattlesCompleted:0, _wavePerfectClear:0, _waveTempAtk:0, _waveTempDef:0` |
| 6 | JS | `loadGame` 缺少波次变量+龙祭变量的默认值守卫 | 添加 `if (G._xxx === undefined) G._xxx = default;` 守卫7条 |
| 7 | JS | 存档版本仍为7，未反映V7→V8迁移 | `version: 7` → `version: 8` |

## 验证结果

| 检查项 | 结果 |
|--------|------|
| CSS括号平衡 | 0 ✅ |
| JS语法 | 通过 ✅ |
| initGame波次变量 | 5/5 ✅ |
| loadGame默认值守卫 | 7/7 ✅ |
| 存档版本 | 8 ✅ |
| calcAtk/calcDef波次buff集成 | 已有 ✅ |
| 109个G._xxx运行时变量 | 大部分有防御性守卫，无crash风险 ✅ |

## V26最终数据

- **行数**: 19039
- **大小**: 854KB
- **函数数**: 324
- **CSS平衡**: 0
- **JS语法**: 通过
- **存档版本**: 8

## 额外发现（未修复，观察中）

- 静态HTML div平衡 ✅（9/9），动态HTML不平衡但属正常（JS生成DOM成对创建）
- onclick `if(confirm(...))` 模式安全，非bug
- 3个switch可能缺break，需人工确认是否有意fall-through
- `_weatherForecast` 和 `DD_CONFIG` 未找到，可能被内联或命名不同
