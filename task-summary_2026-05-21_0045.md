# Task Summary - 2026-05-21 00:45

## Objective
修复手机试玩版HTML(龙途_手机试玩版.html)无法启动的问题

## 根因分析
1. **缺少启动调用**：HTML末尾没有调用`initGame()`和`sceneIntro()`，JS只定义函数但不执行
2. **JS字符串换行符破坏**：之前Python生成脚本直接文本替换时，game_v5.js里的`\n`转义在HTML中被解释为真实换行，导致字符串被截断产生语法错误（SyntaxError: Invalid or unexpected token at line 414）
3. **selectOrigin后未重算maxHp/hp**：选择起源后设置了属性和装备，但G.maxHp/hp仍为初始值100
4. **confirmNameAndAttrs后未调updateStatus**：状态栏不更新

## 修复内容
1. HTML末尾添加 `initGame(); sceneIntro();` 启动调用
2. 改用直接读取game_v5.js源码嵌入HTML的方式（保留`\n`转义），替代之前有bug的Python文本替换
3. `selectOrigin()`末尾添加 `G.maxHp = calcMaxHp(); G.hp = G.maxHp; G.maxMp = calcMaxMp(); G.mp = G.maxMp;`
4. `confirmNameAndAttrs()`添加 `updateStatus();` 调用
5. `sceneSelectOrigin()`添加 `updateStatus();` 调用

## 验证
- Node.js语法检查通过
- JSDOM完整流程测试：标题画面→起源选择→起源故事→起名+属性分配→游戏开始，全部通过
- 最终状态：Lv.1 | ❤️238/238 | 💧68/68 | 💰50，状态栏正确

## 文件
- `game_v5.js` ~154KB（3处修复）
- `龙途_手机试玩版.html` ~166KB（重新生成，可直接分享）
