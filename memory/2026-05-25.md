# 2026-05-25 龙途传说开发日志

## V15 故事选择卡死Bug修复 (23:07-23:18)

### 问题
用户试玩V15，选择命运后界面卡死，无法做任何操作。

### 根因
`selectStoryChoice(effect)` 存 `G.storyChoices[effect]`（如 `combat_bonus`），但 `showStoryChoice(chapterId)` 检查 `G.storyChoices[chapterId]`（如 `ch1_arrival`）。key不匹配 → 选择后 `showStory()` 再次调用 `showStoryChoice()` → 再次弹出选择 → 无限循环卡死。

### 修复3处
1. `selectStoryChoice` 里先存 `G.storyChoices[chapterId] = effect`，标记章节已选择
2. 选择后返回之前场景(`G._currentScene`)而非强制 `showStory()`（避免循环）
3. 选择回顾从STORY_CHOICES反查显示中文文本，不再显示内部effect key

### 文件
- game_v5.js (9546行)
- 龙途_手机试玩版_V15.html (538KB) — 已重新构建，5项检查全通过

### 当前进度卡
战斗96% | 装备95% | 故事65% | 探索90% | 商贸70% | 迷宫70% | 世界55% | 任务50% | 好感55% | 赌博100% | 深潜50% | GDD 50%

## V16 九项Bug修复 (23:18-)

### 已完成修复
1. **饰品槽拆分**: accessory → necklace(项链)+ring(戒指)，7→8槽位。存档自动迁移旧accessory→necklace
2. **NaN百分比保护**: calcCrit/calcDodge中所有value加Number()和||0
3. **铁匠NPC移到铁匠铺**: sceneBlacksmith()添加smithNpcMap，旅店不再显示铁匠NPC
4. **任务进度统一搜索**: _findQuestById()统一搜索QUESTS/guildQuests/SIDE_QUESTS/CHAIN_QUESTS
5. **kill_elite_at进度追踪**: _updateBattleQuestProgress新增处理，检查G.battle.isElite
6. **find_treasure/find_treasure_at统一**: 同时匹配两种progressType
7. **visit_location计数修复**: 从固定1次改为累加，支持progressCount>1
8. **任务描述改进**: explore模板"前往{loc}探索{n}次"
9. **刷新公会任务保留已接取**: refreshGuildQuests保留已接取任务
10. **collect_material进度追踪**: 自动清点背包材料数量

### 文件
- game_v5.js (9604行)
- 龙途_手机试玩版_V16.html (541KB, 5项检查通过)

## V17 四项问题修复 (23:30-23:39)

### 问题1: 装备属性计算统一 ✅
- `calcMaxHp()`/`calcMaxMp()` 统一计算：基础值 + 体魄/智力加成 + 装备加成
- `confirmEquip` 和 `unequip` 正确调用 calcMaxHp/calcMaxMp 更新
- `doLevelUp` 和 `initGame` 里也正确更新 maxHp/maxMp

### 问题2: 独立装备界面 ✅
- 新增 `sceneEquipment()` 独立装备界面，含角色属性面板+已装备列表+背包装备列表
- 装备对比显示（如"钢剑 ATK+22 > 铁剑"）
- 从城镇添加装备入口按钮
- `confirmEquip`/`unequip` 根据来源场景返回装备界面或背包界面
- 新增 `_cancelEquip()` 函数处理取消装备预览

### 问题3: 任务数量不一致修复 ✅
- 城镇banner显示"进行中(N)"用的是 G.quests.length（所有已接取ID），但 activeNames 只从 QUESTS 找
- 修复：用 `_findQuestById` 统一搜索，确保显示数与实际可交互数一致

### 问题4: 任务联动性和故事深度 🔄 进行中
- 当前章节目标太模板化（去X地/打Y怪/升Z级），缺乏叙事深度
- 计划改进：
  - 4a: 章节感知公会任务（当前章节时生成剧情相关任务）
  - 4b: 扩展前3章目标（增加NPC交互、调查、线索类型）
  - 4c: 新增 checkStoryTriggers 目标类型：talk_to_npc / find_clue / collect_evidence
- **尚未开始编码，下次继续**

### 当前进度卡
战斗96% | 装备98% | 故事65% | 探索90% | 商贸70% | 迷宫70% | 世界55% | 任务55% | 好感55% | 赌博100% | 深潜50% | GDD 50%
