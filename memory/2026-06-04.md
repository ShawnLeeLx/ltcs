# 2026-06-04 开发日志

## V23任务系统修复 + 重复代码清理

### 重复代码清理
- V23文件因之前Python脚本替换bug导致13个函数重复（getNpcAffectionTierByMin/showGiftPanel/doGift/closeGiftPanel/scaleEnemy/checkAchievements/sceneAchievements/generateMap/countNeighbors/isConnected/getMapForLocation/getRoom/canMove）
- 精确定位window.onload后的自然结束点（char 544590），删除之后所有重复代码
- 文件从14658行/657KB → 13112行/571KB，0重复函数

### 任务系统补全（70%→100%）
- 新增 `_updateBossQuestProgress(locId)`：Boss击杀时触发defeat_boss_at和side_stages boss阶段进度
- Boss击杀hook：`_updateBossQuestProgress(G.location)` 在bossDefeated赋值后调用
- side_stages进度推进：在3个_update*QuestProgress中添加阶段关键词匹配（战斗/探索/寻物/回报阶段自动推进）
- sceneGuild任务卡片：点击可接/进行中/已完成任务→showQuestDetail详情
- `_showQuestsFromTown()`：冒险日志面板（主线进度+任务进度条+任务链+公会按钮）
- sceneTown任务横幅：点击→_showQuestsFromTown()
- sceneTown新增📖故事建筑入口
- collect_material修复：使用G.materials而非遍历G.inventory

### 好感度系统（85%→90%）
- `sceneNpcRelations()`：NPC关系面板（按城镇分组、好感进度条、tier颜色、喜好/解锁显示）
- `_renderNpcRelationCard()`：关系卡片渲染辅助函数

### 语法修复
- `_showQuestsFromTown`中if/else花括号不匹配+CHAIN_QUESTS显示块多一个}

### 最终状态
- 龙途_手机试玩版_V23.html：13328行/581KB/JS语法通过/0重复函数
- 任务系统100%，好感度90%，故事100%，世界地图100%

## V23好感度系统 90%→100%

### 核心函数补全（4个之前调但未定义）
- `getNpcAffection(npcId)`: 从G.npcRelations读取好感度
- `getNpcAffectionTier(npcId)`: 根据好感度返回tier对象（含title/emoji/color）
- `addNpcAffection(npcId, amount)`: 增加好感+自动触发解锁（40/70/100阈值）
- `giveGiftToNpc(npcId, materialId)`: 送礼逻辑（消耗材料+喜爱2倍+触发解锁）
- `getNpcDiscount(npcId, target)`: 计算NPC折扣（上限50%）

### 解锁兑现系统 `_applyNpcUnlock`
- discount→G._npcDiscounts→getNpcDiscount自动应用
- buff→G._npcBuffs→getEffStat/calcMaxHp/calcAtk/calcDef自动加成
- skill→G.skills.push+G._npcSkills
- recipe→G._unlockedRecipes
- item→G._pendingNpcItems队列

### 送礼入口补全
- sceneShop: 新增🎁送礼按钮（showGiftPanel('shop')）
- sceneGuild: 新增🎁送礼按钮（showGiftPanel('guild')）
- showGiftPanel新增shop/guild两种场景NPC映射表

### 15个NPC好感技能加入SKILLS数组
guardian_stance/dragon_tongue/fate_eye/mist_walk/poison_clear/nature_shield/arcane_sight/storm_step/dragon_breath/iron_formation/frost_armor_skill/dragon_speech/ash_walk/flame_sense/abyss_eye/shadow_stealth

### 数据格式修复
- sceneNpcRelations: NPC_DEFS对象遍历（非数组），用npc.loc替代npc.location
- _renderNpcRelationCard: tier从对象取属性，likes显示中文名，三级解锁分别显示
- completeQuest: NPC_DEFS对象查找修复（从.find改为for-in）

### 战斗属性集成
- getEffStat/calcMaxHp/calcAtk/calcDef均加入G._npcBuffs自动加成

### 最终状态
- 龙途_手机试玩版_V23.html：13551行/591KB/JS语法通过/0重复函数
- 好感度100%，任务系统100%，故事100%，世界地图100%
