# 2026-05-27 Daily Notes

## V18构建完成 (09:44)

### 修复：完成主线任务后地点消失（P0 Bug）
- **根因:** `_restoreLocationUnlocks()`用`G.storyProgress.chapterId`在STORY数组中查找索引重放解锁。旧存档章节ID（如ch1_first_quest）在新STORY中不存在→currentIdx=-1→循环跳过所有解锁→只剩whitecity
- **修复方案:**
  1. 添加`_OLD_CHAPTER_MAP`（6条旧→新ID映射）和`_resolveChapterId()`函数
  2. `_restoreLocationUnlocks()`使用resolved ID查找
  3. 添加等级兜底：currentIdx<0时按等级解锁对应区域地点
  4. `loadGame()`中自动迁移旧章节ID
  5. `CHAPTER_UNLOCKS['ch1_arrival']`改为['lake']——新玩家必须一开始就能去坠星湖

### 修复：任务进度不显示（P1 Bug）
- **根因:** 只有`progressCount`字段存在的任务才显示"进度:X/Y"，单步任务无进度显示
- **修复:**
  1. 默认progressCount=1，所有任务统一显示进度
  2. `_showQuestsFromExplore`改用`_findQuestById`搜索所有任务源（QUESTS+GUILD+SIDE+CHAIN）
  3. 已完成任务显示✅标记

### 构建状态
- V18.html: 554KB, 11455行, 5项检查全过, JS语法无错

### 当前总体进度
战斗96% | 装备95% | 故事55% | 探索90% | 商贸60% | 迷宫70% | 世界55% | 任务35% | 好感55% | 赌博100% | 深潜50% | GDD 50%

### 剩余关键待做项
- ch4~ch12故事目标太模板化（只有visit_location+win_battles+reach_level），需添加更有趣的目标类型
- 铁匠铺NPC（smithNpcMap）与旅店NPC系统的统一（非紧急）
- NPC对话内容需随故事推进更新（目前部分NPC对话固定不变）

## V19修复7项反馈 (10:00-10:02)

### 修复清单
1. **装备穿戴bug** — buyItem()缺少slot/rarityColor/affixes等字段复制；previewEquip/confirmEquip的slotKey fallback错误（非ring/necklace全进necklace），改为8种slot完整映射+null检查
2. **装备入口位置** — 从建筑列表移至底部按钮组，与背包/角色/主线/存档并列
3. **装备界面看不到可穿戴装备** — sceneEquipment过滤条件`type==='equipment'`不匹配商店物品（商店type为'weapon'/'armor'等）；改为同时匹配slot字段和8种装备type；buyItem统一存为type:'equipment'+slot字段
4. **智力显示0** — G.intel→G.int全局修正（sceneEquipment/scenePlayerInfo中）
5. **图标重复** — 冒险公会⚔️→📜，商贸站🏪→🚛，杂货店🏪→🧴
6. **图标风格** — 便利店🏪替换为🧴更符合中世纪
7. **酒馆NPC互动** — 新增_getTavernNpcs()数据函数(6城18个NPC)+tavernNpcChat()对话函数，每城3个NPC各有独特对话和奖励(buff/heal/gold/material/reveal/info 6类)

### 关键代码变更
- buyItem: 购买装备时完整复制所有属性(type:'equipment', slot, rarity, rarityColor, affixes, effects等)
- 背包堆叠+过滤: equipment tab过滤条件扩展匹配8种装备type
- sceneEquipment: bagEq过滤条件同上
- _getTavernNpcs(): 全局函数，6城各3个NPC，每人有name/emoji/title/dialog/reward
- tavernNpcChat(): 对话界面+奖励发放+已聊过标记(G.npc_talked_xxx)
- 城镇底部按钮新增⚔️装备按钮

### 构建
- V19.html: 576KB, 10095行JS, 大括号平衡=0, G.intel残留=0

### 进度更新
装备97% | 世界60% | 好感60% | 其余不变
