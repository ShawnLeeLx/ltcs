# 套装系统完善 — 任务摘要

**时间**：2026-06-08 22:12 ~ 22:21  
**文件**：龙途_手机试玩版_V24.html（802KB / 15160行 / JS语法通过）

## 目标
完善装备套装系统（从50%→95%），兑现套装效果、添加图鉴UI

## 完成内容

### 1. 套装效果兑现
- **幸运商人2件 lukBonus**：`getEffStat('luk')` 中新增 `calcSetBonuses().lukBonus` 加成（+10幸运值）
- **幸运商人4件 shopDiscount**：购买价格乘以 `1.0 - shopDiscount`（9折买），在 `getShopPrice` 中实现
- **火焰领主6件**：`fireCdReduct` 改为 `fireDmgMult:0.25`（火焰伤害+25%），因游戏无冷却系统

### 2. 套装图鉴（renderSetCollection）
- 铁匠铺新增📋套装Tab → `renderSetCollection()` 函数
- 5套装备展示：进度条、2/4/6件加成状态(🟢/⚪)、已装槽位、锻造配方名
- 装备面板也新增📋套装图鉴区域（装备已穿部分 + 背包部分各统计）
- 铁匠锻造页面配方名下方显示📋套装归属

### 3. 锻造setId传递
- `finishCraft()` 新增 `if (recipe.setId) item.setId = recipe.setId;`
- 同时补充 `rarityColor` 和 `icon` 到打造物品

## 关键代码位置（V24行号参考）
- `getEffStat`: ~L6117 — lukBonus在函数末尾
- `getShopPrice`: ~L9505 — shopDiscount/shopMarkup
- `calcSetBonuses`: ~L9212
- `EQUIP_SETS`: ~L9179 — 5套装定义
- `renderSetCollection`: ~L11773 — 套装图鉴
- 铁匠铺Tab: ~L11597 — 新增sets tab
- `finishCraft`: ~L11890 — setId传递
